#include "main.h"

/**
 * print_rev - Function that print string in reverse
 * @s: parameter char s
 * Return: 1
 */

void print_rev(char *s)
{
	int l = 0;

	while (s[i])
		i++;

	while (l--)
		_putchar(s[l]);
	_putchar('\n');
}
